<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pg.bbs.dao.RecommendMapper">
    <resultMap type="com.pg.bbs.entity.Recommend" id="RecommendMap">
        <result property="uuid" column="uuid" jdbcType="VARCHAR"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="excerpt" column="excerpt" jdbcType="VARCHAR"/>
        <result property="imageUrl" column="image_url" jdbcType="VARCHAR"/>
        <result property="userId" column="user_id" jdbcType="VARCHAR"/>
        <result property="labels" column="labels" jdbcType="VARCHAR"/>
        <result property="votes" column="votes" jdbcType="INTEGER"/>
        <result property="viewsWord" column="views_word" jdbcType="INTEGER"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="status" column="status" jdbcType="INTEGER"/>
        <result property="type" column="type" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="modifyTime" column="modify_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <select id="queryAll" resultMap="RecommendMap">
        select
        c.*,
        u.username,
        u.head_pic as headPic
        from
        tb_comment c
        LEFT JOIN
        tb_user u
        ON
        c.user_id = u.uuid
        <where>
            <if test="articleId != null and articleId != ''">
                and c.article_id = #{articleId}
            </if>
            <if test="userId != null and userId != ''">
                and c.user_id = #{userId}
            </if>
        </where>
        ORDER BY c.create_time
    </select>

    <select id="findById" resultMap="RecommendMap">
        select * from tb_recommend where uuid = #{uuid}
    </select>

    <select id="findQueryTitle" resultType="java.lang.Integer">
        select COUNT(title) from tb_recommend where title = #{title}
    </select>

    <insert id="insert">
        insert into  tb_recommend(uuid, title, excerpt, image_url, user_id, labels, votes, views_word, content, status, type, create_time, modify_time)
        values (#{uuid}, #{title}, #{excerpt}, #{imageUrl}, #{userId}, #{labels}, #{votes}, #{viewsWord}, #{content}, #{status}, #{type}, #{createTime}, #{modifyTime})
    </insert>

    <update id="update">
        update tb_recommend
        <set>
            <if test="title != null and title != ''">
                title = #{title},
            </if>
            <if test="excerpt != null and excerpt != ''">
                excerpt = #{excerpt},
            </if>
            <if test="imageUrl != null and imageUrl != ''">
                image_url = #{imageUrl},
            </if>
            <if test="userId != null and userId != ''">
                user_id = #{userId},
            </if>
            <if test="labels != null and labels != ''">
                labels = #{labels},
            </if>
            <if test="votes != null and votes != ''">
                votes = #{votes},
            </if>
            <if test="viewsWord != null and viewsWord != ''">
                views_word = #{viewsWord},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="status != null and status != ''">
                status = #{status},
            </if>
            <if test="type != null and type != ''">
                type = #{type},
            </if>
            <if test="modifyTime != null">
                modify_time = #{modifyTime},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
        </set>
        where uuid = #{uuid}
    </update>

</mapper>